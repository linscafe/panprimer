# Nextflow
.nextflow/
.nextflow.log*
work/
# Python
__pycache__/
*.egg-info/
.pytest_cache/
*.pyc
# Rust / compiled extension (maturin drops the .so into src/pangenome_primer/ on an
# editable install; rust/*/target is cargo's build dir)
*.so
*.pyd
target/
# Results / data
results/
selftest_out/
*.fai
*.bwt
*.amb
*.ann
*.pac
*.sa
*.mmi
*.paf
# Large reference/haplotype data lives outside the repo
hprc-r2/
# Demo (kept local only — samples, run script, and example results)
demo-design-pipeline/
demo-verify-pipeline/
# Reference notes (kept local only)
ref/
# Verify/design run outputs (same rule as results/ above)
verify_out_*/
# Raw run logs and throwaway driver scripts; the distilled timings live in docs/
logs/
